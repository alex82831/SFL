# Copy to .env and fill in to actually send. Port 587 + starttls is the
# usual pairing; 465 wants SMTP_TLS=implicit.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_TLS=starttls
SMTP_USER=app@example.com
SMTP_PASSWORD=app-password
MAIL_FROM="SFL demo <app@example.com>"
MAIL_TO=you@example.org
