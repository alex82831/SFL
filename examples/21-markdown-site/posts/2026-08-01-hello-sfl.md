+++
title = "Hello, SFL"
date = "2026-08-01"
tags = ["sfl", "intro"]
+++

# Why another scripting language?

Because a language you can read *end to end* is a different kind of tool.
SFL's interpreter, compiler and standard packages fit in one head.

## What this post shows

- Markdown with **bold**, *italics* and `code`
- Fenced code blocks:

```sfl
def greet(name) = "hello, ${name}"
```

> Blockquotes work too, and [links](https://github.com/alex82831/SFL).
