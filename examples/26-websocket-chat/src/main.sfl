// A WebSocket chat room. Serve it, then open two browser tabs on
// http://127.0.0.1:8080 and talk to yourself.

import "httpd"
import "chat"

val srv = httpServer({port: 8080, handler: chatApp()})
println("chat on http://127.0.0.1:8080 — open it twice, Ctrl-C stops it")
srv.listen()
