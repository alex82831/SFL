// A library that uses builtins. Whatever the program importing it calls its own
// functions, this file's `map` and `length` are still the builtins.
def doubleAll(xs) = map(xs, (x) -> x * 2)
def size(xs) = length(xs)
def biggest(xs) = max(xs[0], xs[1])
